import React from 'react';
// import { useLocation } from 'react-router-dom';

const Footer = () => {
//   const location = useLocation();

  return(
  <footer className="inset-x-0 bottom-0 text-center bg-black h-10 text-white p-2 mt-10">
        Footer Content
  </footer>
  )
};

export default Footer;
